[toolchain]
# Transitive dependencies in Cargo.lock (cookie_store, time, home, icu_*
# packages) require Rust 1.86-1.88. Pinning the channel here ensures
# builders like Railpack install a compatible toolchain instead of falling
# back to an older default that fails the build.
channel = "1.88.0"
