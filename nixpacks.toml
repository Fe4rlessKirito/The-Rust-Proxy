[phases.setup]
nixPkgs = ["rustc", "cargo", "nodejs_20", "npm"]

[phases.build]
cmds = [
  "npm ci --prefix frontend",
  "npm run build --prefix frontend",
  "cargo build --release"
]

[start]
cmd = "./target/release/leech-rs"
