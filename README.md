# The-Rust-Proxy
⚡ Performance Improvements 4× faster – request handling is snappier, especially under load.  5× less memory – idle memory dropped from ~15 MB to ~3 MB. (only around 2mb at startup ram usage will grow depending on how many accounts are in pool) this is without counting the tor proxies
